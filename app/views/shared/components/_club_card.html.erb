<div class="club-card">

  <h3><%= club.name %></h3>

  <p>
    📱 <%= club.whatsapp_contact_number %>
  </p>

  <%= link_to "Ver club", club_path(club) %>

  <%= link_to "Editar Clube", edit_club_path(club) %>


</div>
